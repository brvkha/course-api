spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course_demo?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt:
  secret: MyVerySecretKeyThatIsAtLeast256BitsLongForHS512AlgorithmToWorkProperly123456789
  expiration: 300000

# Custom config cho AWS Local
aws:
  dynamodb:
    endpoint: http://localhost:8000
    region: ap-southeast-1